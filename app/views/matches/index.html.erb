<div class="matches-container">
  <div class="matches-header">
    <h1 class="matches-title">💬 Seus Matches</h1>
    <p class="matches-subtitle">Converse com pessoas que você curtiu</p>
  </div>

  <% if @matches.any? %>
    <div class="matches-grid">
      <% @matches.each do |match| %>
        <% other = match.other_user(current_user) %>
        
        <div class="match-card">
          <div class="match-card-header">
            <% if other.avatar.attached? %>
              <%= image_tag other.avatar, class: "match-avatar" %>
            <% else %>
              <div class="match-avatar-placeholder">
                <%= other.display_name[0].upcase rescue "?" %>
              </div>
            <% end %>
          </div>

          <div class="match-card-body">
            <h3 class="match-name"><%= other.display_name %></h3>
            <p class="match-bio"><%= truncate(other.bio.presence || "Sem descrição ainda", length: 60) %></p>
            
            <div class="match-card-footer">
              <%= link_to match_path(match), class: "match-chat-btn" do %>
                <span class="chat-icon">💬</span>
                <span class="chat-text">Abrir Chat</span>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="matches-pagination">
      <%= paginate @matches %>
    </div>
  <% else %>
    <div class="matches-empty">
      <div class="empty-icon">💭</div>
      <h3 class="empty-title">Você ainda não tem nenhum match</h3>
      <p class="empty-text">Comece curtindo perfis para encontrar alguém interessante!</p>
      <%= link_to "Descobrir pessoas 🔍", discover_path, class: "btn-discover" %>
    </div>
  <% end %>
</div>
