<h2 class="text-center mt-3">Descubra pessoas próximas</h2>

<div id="map" style="width: 100%; height: 500px; border-radius: 10px;"></div>

<!-- Popup fixo -->
<div id="user-popup" class="user-popup hidden">
  <div class="popup-content">
    <div class="user-info">
      <img id="popup-avatar" src="" alt="Avatar" class="avatar">
      <div>
        <h3 id="popup-username"></h3>
        <p id="popup-location"></p>
        <p id="popup-distance" class="distance"></p>
      </div>
    </div>

    <div class="popup-actions">
      <button id="like-btn" class="btn like">❤️ Curtir</button>
      <button id="message-btn" class="btn message">💬 Mensagem</button>
      <button id="goto-btn" class="btn go">🚶‍♂️ Vou até você / Venha até mim</button>
    </div>
  </div>
</div>

<script type="module" src="<%= asset_path 'map.js' %>"></script>

<style>
  .user-popup {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: #fff;
    border-top-left-radius: 16px;
    border-top-right-radius: 16px;
    box-shadow: 0 -3px 10px rgba(0,0,0,0.15);
    padding: 15px;
    transition: all 0.3s ease;
    transform: translateY(100%);
  }

  .user-popup.show {
    transform: translateY(0);
  }

  .user-info {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .avatar {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    border: 3px solid #00aaff;
    object-fit: cover;
  }

  .popup-actions {
    display: flex;
    justify-content: space-between;
    margin-top: 15px;
  }

  .btn {
    flex: 1;
    margin: 0 4px;
    padding: 10px;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    font-weight: 600;
  }

  .btn.like { background: #ff5a5f; color: white; }
  .btn.message { background: #007bff; color: white; }
  .btn.go { background: #002bff; color: white; }

  .hidden { display: none; }
</style>
